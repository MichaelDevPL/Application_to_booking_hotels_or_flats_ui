import { OffersOderByPipe } from './offers-oder-by.pipe';

describe('OffersOderByPipe', () => {
  it('create an instance', () => {
    const pipe = new OffersOderByPipe();
    expect(pipe).toBeTruthy();
  });
});
